# if pidof -x "a.sh" >/dev/null; then
#     echo "Process already running"
# fi

# echo $$
# ps -q $$
# sleep 1

# pid=$(pgrep a.sh)
# echo $pid
# if pidof -x "a.sh" >/dev/null; then
#     echo "Process already running"
# fi
# echo "$(pgrep -f a.sh)" | wc -l

# if [[ "$(pgrep -f a.sh)" != "" ]]; then
#     kill $$
# fi
echo true
# sleep 10
